<h1>Add a clinic</h1>

<%= render "form", user: @user, clinic: @clinic %>

<div class="d-flex justify-content-start text-center mt-5">
  <%= link_to '← Back', request.referer || root_path, class: "btn btn-outline-secondary" %>
</div>
